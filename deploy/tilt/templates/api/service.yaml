apiVersion: v1
kind: Service
metadata:
  name: {{ include "tilt.fullname" . }}
  labels:
    {{- include "tilt.apiLabels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tilt.apiSelectorLabels" . | nindent 4 }}
